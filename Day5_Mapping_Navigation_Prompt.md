# Day 5 — Market Mapping & Navigation Prompt (Send this to Claude)

**PROMPT TO CLAUDE (Day 5):**

> "Today, implement the navigation logic.
>
> **Tasks:**
> 1. Create market map data structure (e.g., graph of stalls and vendor locations).
> 2. Generate vendor coordinates (latitude/longitude or map grid).
> 3. Implement shortest‑path algorithm (Dijkstra or A*).
> 4. Build the `/navigate` FastAPI endpoint that accepts `from` and `to` query parameters.
> 5. Return route steps and a polyline (or list of coordinates) suitable for rendering on a map.
> 6. Write unit tests for the path‑finding function.
>
> **Deliverable:** A concise implementation guide (Markdown) with code snippets for data structures, algorithm, API definition, and testing.
