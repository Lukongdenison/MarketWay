# Day 4 — Implement Whisper Voice Search Prompt (Send this to Claude)

**PROMPT TO CLAUDE (Day 4):**

> "Today, build the complete voice search system.
>
> **Tasks:**
> 1. Integrate Whisper model (Base or Small) using `faster-whisper` or `whisper`.
> 2. Create a function to convert audio files to text.
> 3. Connect Whisper output to MiniLM semantic search pipeline.
> 4. Handle various accents, background noise, and different audio formats.
> 5. Build the `/voice-search` FastAPI endpoint.
> 6. Test the endpoint with sample audio inputs.
> 7. Return search results in JSON format.
>
> **Deliverable:** A step‑by‑step implementation guide (Markdown) with code snippets for model loading, audio processing, API definition, and testing instructions.
